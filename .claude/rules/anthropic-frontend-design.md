---
paths:
  - 'src/**/*.{tsx,css}'
---

# Moved to /DESIGN.md

Visual identity, typography, color, components, motion, and anti-patterns now live in [/DESIGN.md](../../DESIGN.md) — the single source of truth for the design system. Read it before generating or editing any UI.
